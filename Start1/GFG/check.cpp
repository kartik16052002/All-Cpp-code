#include<bits/stdc++.h>
using namespace std;


int main(){

    int n=27;
    double x=3;
    double r = pow(n,1/x);
    cout<<r<<" ";
        int rg = (int)r;
        cout<<"rg is: "<<rg<<"\n";
        int arr[rg] = {0};

        // for(int i=0;i<=rg;i++){
        //     arr[i] = pow(i+1,x);
        //     cout<<arr[i]<<" ";
        // }
}